"use client";
import { calcLength, motion } from "framer-motion";
export default function Sidebar() {
  return (
    <motion.div className=" h-full bg-secondary flex items-center justify-center text-secondary-foreground">
      hello there
    </motion.div>
  );
}
